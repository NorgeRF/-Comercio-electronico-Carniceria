<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        /* ESTILOS INLINE PARA GARANTIZAR VISUALIZACIÓN INMEDIATA */
        :root {
            --primary-color: #c00;
            --secondary-color: #333;
            --light-bg: #f9f9f9;
            --white: #fff;
            --shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Roboto', sans-serif;
            line-height: 1.6;
            color: #333;
            background: #fff;
            padding-top: 80px; /* ESPACIO PARA HEADER FIXED */
        }
        
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* HEADER FIXED CON FONDO SÓLIDO */
        .main-header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: var(--white);
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
            z-index: 1000;
            padding: 0;
        }
        
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            min-height: 80px;
        }
        
        .nav-brand .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.6rem;
            font-weight: 700;
            color: var(--primary-color);
            text-decoration: none;
        }
        
        .nav-brand .logo i {
            font-size: 2.2rem;
        }
        
        .nav-menu {
            display: flex;
            gap: 20px;
            align-items: center;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 18px;
            text-decoration: none;
            color: var(--secondary-color);
            font-weight: 500;
            border-radius: 6px;
            transition: all 0.3s ease;
            font-size: 0.95rem;
        }
        
        .nav-link:hover {
            background: rgba(200, 0, 0, 0.08);
            color: var(--primary-color);
        }
        
        .nav-link.active {
            background: var(--primary-color);
            color: var(--white);
        }
        
        /* DROPDOWN ESTILO MODERNO */
        .dropdown {
            position: relative;
        }
        
        .dropdown-menu {
            position: absolute;
            top: 100%;
            right: 0;
            background: var(--white);
            min-width: 220px;
            box-shadow: var(--shadow);
            border-radius: 8px;
            padding: 8px 0;
            opacity: 0;
            visibility: hidden;
            transform: translateY(10px);
            transition: all 0.3s ease;
            z-index: 1001;
            border: 1px solid rgba(0,0,0,0.08);
        }
        
        .dropdown:hover .dropdown-menu {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }
        
        .dropdown-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 12px 20px;
            color: var(--secondary-color);
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .dropdown-item:hover {
            background: #f8f8f8;
            color: var(--primary-color);
        }
        
        .dropdown-divider {
            margin: 0.5rem 0;
            border-top: 1px solid rgba(0,0,0,0.1);
        }
        
        /* BOTÓN WHATSAPP FLOTANTE */
        .whatsapp-float {
            position: fixed;
            bottom: 25px;
            right: 25px;
            background: #25D366;
            color: white;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 30px;
            box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4);
            z-index: 999;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .whatsapp-float:hover {
            transform: scale(1.1);
            background: #128C7E;
        }
        
        /* HERO SECTION - AHORA CON MARGEN CORRECTO */
        .hero {
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url('/images/hero-bg.jpg');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 100px 0;
            margin-top: 0;
            text-align: center;
            position: relative;
        }
        
        .hero-content h2 {
            font-family: 'Poppins', sans-serif;
            font-size: 3rem;
            margin-bottom: 20px;
            font-weight: 700;
        }
        
        .hero-content p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }
        
        .hero-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 30px;
        }
        
        .btn {
            padding: 14px 30px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            border: none;
            cursor: pointer;
            font-size: 1rem;
        }
        
        .btn-primary {
            background: var(--primary-color);
            color: white;
        }
        
        .btn-primary:hover {
            background: #a00;
            transform: translateY(-3px);
            box-shadow: 0 7px 15px rgba(200,0,0,0.3);
        }
        
        .btn-secondary {
            background: transparent;
            color: white;
            border: 2px solid white;
        }
        
        .btn-secondary:hover {
            background: white;
            color: #333;
        }
        
        /* NUEVA SECCIÓN: PRODUCTOS DESLIZANTES - MÁS PEQUEÑOS Y FUNCIONAL */
        .slider-section {
            padding: 50px 0;
            background: var(--light-bg);
            position: relative;
        }
        
        .slider-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .slider-header h2 {
            margin-bottom: 0;
            text-align: left;
            font-size: 2rem;
            color: var(--secondary-color);
        }
        
        .slider-controls {
            display: flex;
            gap: 10px;
        }
        
        .slider-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: white;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1.2rem;
        }
        
        .slider-btn:hover:not(:disabled) {
            background: var(--primary-color);
            color: white;
        }
        
        .slider-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }
        
        .slider-container {
            position: relative;
            overflow: hidden;
            padding: 10px 0;
        }
        
        .slider-track {
            display: flex;
            gap: 15px;
            transition: transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            will-change: transform;
        }
        
        .slider-card {
            flex: 0 0 180px; /* TAMAÑO FIJO MÁS PEQUEÑO */
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
        }
        
        .slider-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.15);
        }
        
        /* CONTENEDOR DE IMAGENES DEL SLIDER - CORREGIDO */
        .slider-image {
            height: 100px; /* MÁS PEQUEÑO */
            background: #f8f8f8;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 15px;
            position: relative;
            overflow: hidden;
        }
        
        /* ESTILOS CORREGIDOS PARA IMÁGENES DEL SLIDER */
        .slider-image.has-image {
            background: #f8f8f8;
            padding: 0;
        }
        
        .slider-image.no-image {
            background: linear-gradient(135deg, #f8f8f8, #e8e8e8);
            color: var(--primary-color);
        }
        
        .slider-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }
        
        .slider-card:hover .slider-image img {
            transform: scale(1.05);
        }
        
        .slider-image i {
            font-size: 1.8rem; /* MÁS PEQUEÑO */
            color: var(--primary-color);
            display: none; /* OCULTO POR DEFECTO */
        }
        
        /* Mostrar icono solo cuando no hay imagen o la imagen falla */
        .slider-image.no-image i {
            display: block;
        }
        
        /* Cuando la imagen falla */
        .slider-image img.error {
            display: none;
        }
        
        .slider-image img.error ~ i {
            display: block;
        }
        
        .slider-info {
            padding: 12px;
            text-align: center;
        }
        
        .slider-info h4 {
            font-family: 'Poppins', sans-serif;
            font-size: 0.9rem; /* MÁS PEQUEÑO */
            margin-bottom: 5px;
            color: var(--secondary-color);
            line-height: 1.3;
            height: 2.6em;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
        }
        
        .slider-price {
            font-size: 1rem; /* MÁS PEQUEÑO */
            font-weight: 700;
            color: var(--primary-color);
            margin: 6px 0;
        }
        
        .slider-card .btn {
            padding: 6px 10px;
            font-size: 0.75rem;
            min-height: auto;
            width: 100%;
        }
        
        /* SECCIONES GENERALES */
        section {
            padding: 80px 0;
        }
        
        h2 {
            font-family: 'Poppins', sans-serif;
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 50px;
            color: var(--secondary-color);
        }
        
        /* CATEGORÍAS */
        .categories {
            background: white;
        }
        
        .category-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }
        
        .category-card {
            background: var(--light-bg);
            padding: 40px 30px;
            border-radius: 10px;
            text-align: center;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
        }
        
        .category-card:hover {
            transform: translateY(-10px);
        }
        
        .category-card i {
            font-size: 3rem;
            color: var(--primary-color);
            margin-bottom: 20px;
        }
        
        /* PRODUCTOS DESTACADOS (los de abajo) - IMÁGENES CORREGIDAS */
        .featured-products {
            background: white;
        }
        
        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }
        
        .product-card {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
        }
        
        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }
        
        /* CONTENEDOR DE IMAGENES PRINCIPALES - CORREGIDO */
        .product-image {
            background: #f8f8f8;
            padding: 40px 20px;
            text-align: center;
            height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }
        
        /* ESTILOS CORREGIDOS PARA IMÁGENES PRINCIPALES */
        .product-image.has-image {
            background: #f8f8f8;
            padding: 0;
        }
        
        .product-image.no-image {
            background: linear-gradient(135deg, #f8f8f8, #e8e8e8);
            color: var(--primary-color);
            padding: 40px 20px; /* Mantener padding solo cuando no hay imagen */
        }
        
        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }
        
        .product-card:hover .product-image img {
            transform: scale(1.05);
        }
        
        .product-image i {
            font-size: 3.5rem;
            color: var(--primary-color);
            display: none; /* OCULTO POR DEFECTO */
        }
        
        /* Mostrar icono solo cuando no hay imagen o la imagen falla */
        .product-image.no-image i {
            display: block;
        }
        
        /* Cuando la imagen falla */
        .product-image img.error {
            display: none;
        }
        
        .product-image img.error ~ i {
            display: block;
        }
        
        .product-info {
            padding: 25px;
        }
        
        .product-price {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
            margin: 15px 0;
        }
        
        /* PASOS */
        .how-it-works {
            background: var(--light-bg);
        }
        
        .steps {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 30px;
            text-align: center;
        }
        
        .step-number {
            width: 60px;
            height: 60px;
            background: var(--primary-color);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            font-weight: 700;
            margin: 0 auto 20px;
        }
        
        /* CONTACTO */
        .contact-section {
            background: white;
        }
        
        .contact-section .container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 50px;
            align-items: start;
        }
        
        .contact-info p {
            display: flex;
            align-items: center;
            gap: 15px;
            margin: 20px 0;
            font-size: 1.1rem;
        }
        
        .social-links {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }
        
        .social-links a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            background: #f0f0f0;
            border-radius: 50%;
            color: #333;
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }
        
        .social-links a:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-5px);
        }
        
        .contact-form input,
        .contact-form textarea {
            width: 100%;
            padding: 15px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-family: 'Roboto', sans-serif;
            font-size: 1rem;
        }
        
        /* FOOTER */
        .footer {
            background: #222;
            color: white;
            padding: 60px 0 0;
        }
        
        .footer .container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }
        
        .footer-section h3 {
            font-size: 1.3rem;
            margin-bottom: 20px;
            color: white;
        }
        
        .footer-section a {
            display: block;
            color: #ccc;
            text-decoration: none;
            margin-bottom: 10px;
            transition: color 0.3s ease;
        }
        
        .footer-section a:hover {
            color: white;
        }
        
        .payment-methods {
            display: flex;
            gap: 20px;
            font-size: 2rem;
        }
        
        .footer-bottom {
            text-align: center;
            padding: 20px;
            margin-top: 40px;
            border-top: 1px solid #444;
        }
        
        /* RESPONSIVE */
        @media (max-width: 1200px) {
            .slider-card {
                flex: 0 0 160px; /* Más pequeño en pantallas grandes */
            }
        }
        
        @media (max-width: 992px) {
            body {
                padding-top: 70px;
            }
            
            .nav-menu {
                position: fixed;
                top: 70px;
                left: 0;
                width: 100%;
                background: white;
                flex-direction: column;
                padding: 20px;
                box-shadow: 0 10px 20px rgba(0,0,0,0.1);
                transform: translateY(-100%);
                opacity: 0;
                visibility: hidden;
                transition: all 0.3s ease;
                z-index: 999;
            }
            
            .nav-menu.active {
                transform: translateY(0);
                opacity: 1;
                visibility: visible;
            }
            
            .menu-toggle {
                display: block;
                background: none;
                border: none;
                font-size: 1.5rem;
                color: #333;
                cursor: pointer;
            }
            
            .dropdown-menu {
                position: static;
                box-shadow: none;
                background: #f9f9f9;
                margin-top: 10px;
                opacity: 1;
                visibility: visible;
                display: none;
            }
            
            .dropdown.open .dropdown-menu {
                display: block;
            }
            
            .hero-content h2 {
                font-size: 2.2rem;
            }
            
            .hero-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .slider-card {
                flex: 0 0 140px; /* Más pequeño en tablet */
            }
            
            .slider-header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
            
            .slider-header h2 {
                text-align: center;
            }
        }
        
        @media (max-width: 768px) {
            section {
                padding: 60px 0;
            }
            
            h2 {
                font-size: 2rem;
            }
            
            .slider-card {
                flex: 0 0 120px; /* Más pequeño en móvil */
            }
            
            .slider-image {
                height: 80px;
            }
            
            .slider-image i {
                font-size: 1.5rem;
            }
            
            .slider-info h4 {
                font-size: 0.8rem;
            }
            
            .slider-price {
                font-size: 0.9rem;
            }
            
            .product-image {
                height: 180px;
            }
            
            .product-image i {
                font-size: 3rem;
            }
        }
        
        @media (max-width: 480px) {
            .hero {
                padding: 70px 0;
            }
            
            .hero-content h2 {
                font-size: 1.8rem;
            }
            
            .slider-section {
                padding: 30px 0;
            }
            
            .slider-card {
                flex: 0 0 100px; /* Muy pequeño en móvil pequeño */
            }
            
            .slider-header h2 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- WhatsApp Floating Button -->
    <a href="https://wa.me/<%= whatsapp_number %>?text=Hola,%20me%20interesan%20sus%20productos" 
       class="whatsapp-float" 
       target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

    <!-- Header Fixed -->
    <header class="main-header">
      <div class="container">
        <nav class="navbar">
          <div class="nav-brand">
            <a href="/" class="logo">
              <i class="fas fa-utensils"></i>
              <span>Carnicería Tenerife</span>
            </a>
          </div>
          
          <!-- Botón menú móvil -->
          <button class="menu-toggle" id="menuToggle">
            <i class="fas fa-bars"></i>
          </button>
          
          <div class="nav-menu" id="navMenu">
            <a href="/" class="nav-link <%= (current === 'home') ? 'active' : '' %>">
              <i class="fas fa-home"></i> Inicio
            </a>
            <a href="/productos" class="nav-link <%= (current === 'products') ? 'active' : '' %>">
              <i class="fas fa-drumstick-bite"></i> Productos
            </a>
            <a href="/pedidos" class="nav-link <%= (current === 'orders') ? 'active' : '' %>">
              <i class="fas fa-shopping-cart"></i> Hacer Pedido
            </a>
            <a href="/contacto" class="nav-link <%= (current === 'contact') ? 'active' : '' %>">
              <i class="fas fa-envelope"></i> Contacto
            </a>
            
            <% if (customer) { %>
              <!-- Cliente logueado -->
              <div class="dropdown" id="userDropdown">
                <a href="#" class="nav-link dropdown-toggle">
                  <i class="fas fa-user"></i> Mi Cuenta
                </a>
                <ul class="dropdown-menu">
                  <li><a class="dropdown-item" href="/mi-cuenta">
                    <i class="fas fa-user-circle"></i> Mi Perfil
                  </a></li>
                  <li><a class="dropdown-item" href="/mis-pedidos">
                    <i class="fas fa-shopping-bag"></i> Mis Pedidos
                  </a></li>
                  <li><hr class="dropdown-divider"></hr></li>
                  <li><a class="dropdown-item" href="/logout-cliente">
                    <i class="fas fa-sign-out-alt"></i> Cerrar Sesión
                  </a></li>
                </ul>
              </div>
            <% } else { %>
              <!-- Cliente NO logueado -->
              <a href="/login-cliente" class="nav-link">
                <i class="fas fa-sign-in-alt"></i> Acceder
              </a>
            <% } %>
          </div>
        </nav>
      </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <h2>Los mejores cortes de carne en Tenerife</h2>
                <p>Calidad garantizada, entregas a domicilio y servicio personalizado</p>
                <div class="hero-buttons">
                    <a href="/productos" class="btn btn-primary">Ver Productos</a>
                    <a href="/pedidos" class="btn btn-secondary">Hacer Pedido</a>
                </div>
            </div>
        </div>
    </section>

    <!-- NUEVA SECCIÓN: Productos Deslizantes MÁS PEQUEÑOS Y FUNCIONAL -->
    <section class="slider-section">
        <div class="container">
            <div class="slider-header">
                <h2>Productos Populares</h2>
                <div class="slider-controls">
                    <button class="slider-btn prev-btn">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    <button class="slider-btn next-btn">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>
            
            <div class="slider-container">
                <div class="slider-track" id="sliderTrack">
                    <!-- Los productos se insertarán aquí con JavaScript -->
                    <% if (products && products.length > 0) { %>
                        <% products.forEach(product => { 
                            // Determinar si tiene imagen
                            const hasImage = product.imagen && product.imagen.trim() !== '';
                            const imageClass = hasImage ? 'has-image' : 'no-image';
                        %>
                            <div class="slider-card">
                                <div class="slider-image <%= imageClass %>">
                                    <% if (hasImage) { %>
                                        <img src="<%= product.imagen %>" 
                                             alt="<%= product.nombre %>"
                                             onerror="handleImageError(this)">
                                        <i class="fas fa-<%= getProductIcon(product.categoria) %>"></i>
                                    <% } else { %>
                                        <i class="fas fa-<%= getProductIcon(product.categoria) %>"></i>
                                    <% } %>
                                </div>
                                <div class="slider-info">
                                    <h4><%= product.nombre %></h4>
                                    <p style="font-size: 0.75rem; color: #666; margin-bottom: 5px; line-height: 1.3;">
                                        <%= product.descripcion ? product.descripcion.substring(0, 40) + '...' : 'Producto calidad' %>
                                    </p>
                                    <div class="slider-price">€<%= product.precio.toFixed(2) %>/<%= product.unidad %></div>
                                    <button class="btn btn-primary add-to-slider" 
                                            style="padding: 6px 10px; font-size: 0.75rem; min-height: auto;"
                                            data-id="<%= product.id %>"
                                            data-name="<%= product.nombre %>"
                                            data-price="<%= product.precio %>"
                                            data-unit="<%= product.unidad %>">
                                        <i class="fas fa-cart-plus"></i> Añadir
                                    </button>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <p style="text-align: center; width: 100%; padding: 40px; color: #666;">
                            No hay productos disponibles
                        </p>
                    <% } %>
                </div>
            </div>
        </div>
    </section>

    <!-- Resto del contenido se mantiene igual... -->
    <!-- Product Categories -->
    <section class="categories">
        <div class="container">
            <h2>Nuestras Categorías</h2>
            <div class="category-grid">
                <div class="category-card">
                    <i class="fas fa-cow"></i>
                    <h3>Carnes Frescas</h3>
                    <p>Vacuno, cerdo, pollo y cordero</p>
                </div>
                <div class="category-card">
                    <i class="fas fa-hamburger"></i>
                    <h3>Productos Elaborados</h3>
                    <p>Hamburguesas, salchichas y embutidos</p>
                </div>
                <div class="category-card">
                    <i class="fas fa-fish"></i>
                    <h3>Mariscos</h3>
                    <p>Productos del mar frescos</p>
                </div>
                <div class="category-card">
                    <i class="fas fa-cheese"></i>
                    <h3>Quesos y Acompañamientos</h3>
                    <p>Perfectos para tus carnes</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Featured Products (los de abajo) -->
    <section class="featured-products">
        <div class="container">
            <h2>Productos Destacados</h2>
            <div class="products-grid">
                <% if (products && products.length > 0) { %>
                    <% products.forEach(product => { 
                        // Determinar si tiene imagen
                        const hasImage = product.imagen && product.imagen.trim() !== '';
                        const imageClass = hasImage ? 'has-image' : 'no-image';
                    %>
                        <div class="product-card">
                            <div class="product-image <%= imageClass %>">
                                <% if (hasImage) { %>
                                    <img src="<%= product.imagen %>" 
                                         alt="<%= product.nombre %>"
                                         onerror="handleImageError(this)">
                                    <i class="fas fa-<%= getProductIcon(product.categoria) %>"></i>
                                <% } else { %>
                                    <i class="fas fa-<%= getProductIcon(product.categoria) %>"></i>
                                <% } %>
                            </div>
                            <div class="product-info">
                                <h3><%= product.nombre %></h3>
                                <p><%= product.descripcion || 'Producto de calidad premium' %></p>
                                <div class="product-price">€<%= product.precio.toFixed(2) %>/<%= product.unidad %></div>
                                <button class="btn btn-primary add-to-cart" 
                                        data-id="<%= product.id %>"
                                        data-name="<%= product.nombre %>"
                                        data-price="<%= product.precio %>"
                                        data-unit="<%= product.unidad %>">
                                    <i class="fas fa-cart-plus"></i> Añadir al carrito
                                </button>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p class="no-products" style="text-align: center; width: 100%; padding: 40px; color: #666;">
                        No hay productos destacados disponibles.
                    </p>
                <% } %>
            </div>
        </div>
    </section>

    <!-- How It Works -->
    <section class="how-it-works">
        <div class="container">
            <h2>¿Cómo funciona?</h2>
            <div class="steps">
                <div class="step">
                    <div class="step-number">1</div>
                    <h3>Elige tus productos</h3>
                    <p>Selecciona entre nuestra amplia variedad</p>
                </div>
                <div class="step">
                    <div class="step-number">2</div>
                    <h3>Realiza tu pedido</h3>
                    <p>Por WhatsApp o en nuestra web</p>
                </div>
                <div class="step">
                    <div class="step-number">3</div>
                    <h3>Pago seguro</h3>
                    <p>Tarjeta, transferencia o efectivo</p>
                </div>
                <div class="step">
                    <div class="step-number">4</div>
                    <h3>Entrega a domicilio</h3>
                    <p>En Tenerife en 24-48 horas</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section">
        <div class="container">
            <div class="contact-info">
                <h2>Contacta con nosotros</h2>
                <p><i class="fas fa-phone"></i> Teléfono: 922 123 456</p>
                <p><i class="fas fa-map-marker-alt"></i> Dirección: C/ Ejemplo, 123, Tenerife</p>
                <p><i class="fas fa-clock"></i> Horario: L-V 8:00-20:00, S 8:00-15:00</p>
                <div class="social-links">
                    <a href="#"><i class="fab fa-facebook"></i></a>
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="https://wa.me/<%= whatsapp_number %>"><i class="fab fa-whatsapp"></i></a>
                </div>
            </div>
            <div class="contact-form">
                <h3>Envíanos un mensaje</h3>
                <form id="contact-form">
                    <input type="text" placeholder="Nombre" required>
                    <input type="email" placeholder="Email" required>
                    <textarea placeholder="Mensaje" rows="4" required></textarea>
                    <button type="submit" class="btn btn-primary">Enviar Mensaje</button>
                </form>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-section">
                <h3>Carnicería Tenerife</h3>
                <p>Productos cárnicos de máxima calidad desde 1990</p>
            </div>
            <div class="footer-section">
                <h3>Enlaces Rápidos</h3>
                <a href="/">Inicio</a>
                <a href="/productos">Productos</a>
                <a href="/pedidos">Pedidos</a>
                <a href="/contacto">Contacto</a>
            </div>
            <div class="footer-section">
                <h3>Métodos de Pago</h3>
                <div class="payment-methods">
                    <i class="fab fa-cc-visa"></i>
                    <i class="fab fa-cc-mastercard"></i>
                    <i class="fab fa-cc-paypal"></i>
                    <i class="fas fa-university"></i>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; <%= currentYear %> Carnicería Tenerife. Todos los derechos reservados.</p>
        </div>
    </footer>

    <script>
        // Función para manejar errores en imágenes
        function handleImageError(img) {
            img.style.display = 'none';
            const icon = img.nextElementSibling;
            if (icon && icon.tagName === 'I') {
                icon.style.display = 'block';
                // También cambiar la clase del contenedor
                const container = img.parentElement;
                container.classList.remove('has-image');
                container.classList.add('no-image');
            }
        }
        
        // Función para obtener icono según categoría
        function getProductIcon(category) {
            const icons = {
                'vacuno': 'cow',
                'cerdo': 'piggy-bank',
                'pollo': 'drumstick-bite',
                'cordero': 'paw',
                'elaborados': 'hamburger',
                'mariscos': 'fish',
                'quesos': 'cheese'
            };
            return icons[category.toLowerCase()] || 'shopping-bag';
        }
        
        // Menú móvil
        document.addEventListener('DOMContentLoaded', function() {
            const menuToggle = document.getElementById('menuToggle');
            const navMenu = document.getElementById('navMenu');
            const userDropdown = document.getElementById('userDropdown');
            
            // Toggle menú móvil
            if (menuToggle && navMenu) {
                menuToggle.addEventListener('click', function() {
                    navMenu.classList.toggle('active');
                    this.innerHTML = navMenu.classList.contains('active') 
                        ? '<i class="fas fa-times"></i>' 
                        : '<i class="fas fa-bars"></i>';
                });
                
                // Cerrar menú al hacer click en enlace
                document.querySelectorAll('.nav-link').forEach(link => {
                    link.addEventListener('click', () => {
                        navMenu.classList.remove('active');
                        menuToggle.innerHTML = '<i class="fas fa-bars"></i>';
                    });
                });
            }
            
            // Dropdown para móviles
            if (userDropdown) {
                const dropdownToggle = userDropdown.querySelector('.dropdown-toggle');
                const dropdownMenu = userDropdown.querySelector('.dropdown-menu');
                
                dropdownToggle.addEventListener('click', function(e) {
                    if (window.innerWidth <= 992) {
                        e.preventDefault();
                        userDropdown.classList.toggle('open');
                    }
                });
            }
            
            // Cerrar menú al hacer click fuera
            document.addEventListener('click', function(e) {
                if (!e.target.closest('.navbar') && window.innerWidth <= 992) {
                    navMenu.classList.remove('active');
                    menuToggle.innerHTML = '<i class="fas fa-bars"></i>';
                }
            });
            
            // Añadir al carrito (tanto del slider como del grid)
            document.querySelectorAll('.add-to-cart, .add-to-slider').forEach(button => {
                button.addEventListener('click', function() {
                    const product = {
                        id: this.dataset.id,
                        name: this.dataset.name,
                        price: this.dataset.price,
                        unit: this.dataset.unit
                    };
                    
                    // Mostrar notificación
                    showNotification(`✅ ${product.name} añadido al carrito`, 'success');
                    
                    // Aquí agregarías la lógica para el carrito de compras
                    console.log('Producto añadido:', product);
                });
            });
            
            // Formulario de contacto
            const contactForm = document.getElementById('contact-form');
            if (contactForm) {
                contactForm.addEventListener('submit', function(e) {
                    e.preventDefault();
                    alert('¡Mensaje enviado! Te contactaremos pronto.');
                    this.reset();
                });
            }
            
            // SLIDER DE PRODUCTOS - VERSIÓN CORREGIDA Y FUNCIONAL
            const sliderTrack = document.getElementById('sliderTrack');
            const prevBtn = document.querySelector('.prev-btn');
            const nextBtn = document.querySelector('.next-btn');
            
            if (sliderTrack && prevBtn && nextBtn) {
                let currentPosition = 0;
                let cardWidth = 180 + 15; // Ancho de tarjeta + gap (180px + 15px)
                const sliderContainer = sliderTrack.parentElement;
                let visibleCards = Math.floor(sliderContainer.offsetWidth / cardWidth);
                const totalCards = sliderTrack.children.length;
                let maxPosition = Math.max(0, (totalCards - visibleCards) * cardWidth);
                
                // Función para actualizar slider
                function updateSlider() {
                    sliderTrack.style.transform = `translateX(-${currentPosition}px)`;
                    
                    // Deshabilitar botones si estamos en los extremos
                    prevBtn.disabled = currentPosition <= 0;
                    nextBtn.disabled = currentPosition >= maxPosition || totalCards <= visibleCards;
                    
                    prevBtn.style.opacity = prevBtn.disabled ? '0.5' : '1';
                    nextBtn.style.opacity = nextBtn.disabled ? '0.5' : '1';
                    prevBtn.style.cursor = prevBtn.disabled ? 'not-allowed' : 'pointer';
                    nextBtn.style.cursor = nextBtn.disabled ? 'not-allowed' : 'pointer';
                }
                
                // Botón siguiente - AHORA FUNCIONAL
                nextBtn.addEventListener('click', function() {
                    if (!this.disabled) {
                        currentPosition += cardWidth * visibleCards;
                        if (currentPosition > maxPosition) {
                            currentPosition = maxPosition;
                        }
                        updateSlider();
                    }
                });
                
                // Botón anterior - AHORA FUNCIONAL
                prevBtn.addEventListener('click', function() {
                    if (!this.disabled) {
                        currentPosition -= cardWidth * visibleCards;
                        if (currentPosition < 0) {
                            currentPosition = 0;
                        }
                        updateSlider();
                    }
                });
                
                // Auto-desplazamiento cada 5 segundos
                let autoSlide = setInterval(() => {
                    if (currentPosition >= maxPosition || totalCards <= visibleCards) {
                        currentPosition = 0;
                    } else {
                        currentPosition += cardWidth * visibleCards;
                        if (currentPosition > maxPosition) {
                            currentPosition = maxPosition;
                        }
                    }
                    updateSlider();
                }, 5000);
                
                // Pausar auto-desplazamiento al hover
                sliderTrack.addEventListener('mouseenter', () => {
                    clearInterval(autoSlide);
                });
                
                sliderTrack.addEventListener('mouseleave', () => {
                    autoSlide = setInterval(() => {
                        if (currentPosition >= maxPosition || totalCards <= visibleCards) {
                            currentPosition = 0;
                        } else {
                            currentPosition += cardWidth * visibleCards;
                            if (currentPosition > maxPosition) {
                                currentPosition = maxPosition;
                            }
                        }
                        updateSlider();
                    }, 5000);
                });
                
                // Actualizar al redimensionar ventana
                window.addEventListener('resize', function() {
                    // Recalcular ancho de tarjeta según tamaño actual
                    const computedStyle = window.getComputedStyle(document.querySelector('.slider-card'));
                    cardWidth = parseInt(computedStyle.width) + 15; // + gap
                    visibleCards = Math.floor(sliderContainer.offsetWidth / cardWidth);
                    maxPosition = Math.max(0, (totalCards - visibleCards) * cardWidth);
                    
                    // Ajustar posición actual si es necesario
                    if (currentPosition > maxPosition) {
                        currentPosition = maxPosition;
                    }
                    
                    updateSlider();
                });
                
                // Inicializar slider
                updateSlider();
                
                // Añadir detección de touch para móviles
                let isDragging = false;
                let startPos = 0;
                let currentTranslate = 0;
                let prevTranslate = 0;
                
                sliderTrack.addEventListener('touchstart', touchStart);
                sliderTrack.addEventListener('touchend', touchEnd);
                sliderTrack.addEventListener('touchmove', touchMove);
                
                function touchStart(e) {
                    isDragging = true;
                    startPos = e.touches[0].clientX;
                    sliderTrack.style.transition = 'none';
                }
                
                function touchEnd() {
                    isDragging = false;
                    sliderTrack.style.transition = 'transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
                    
                    const movedBy = currentTranslate - prevTranslate;
                    
                    if (movedBy < -50 && currentPosition < maxPosition) {
                        // Deslizar a la derecha
                        currentPosition += cardWidth * visibleCards;
                        if (currentPosition > maxPosition) currentPosition = maxPosition;
                    }
                    
                    if (movedBy > 50 && currentPosition > 0) {
                        // Deslizar a la izquierda
                        currentPosition -= cardWidth * visibleCards;
                        if (currentPosition < 0) currentPosition = 0;
                    }
                    
                    updateSlider();
                }
                
                function touchMove(e) {
                    if (!isDragging) return;
                    e.preventDefault();
                    const currentPositionX = e.touches[0].clientX;
                    currentTranslate = prevTranslate + currentPositionX - startPos;
                    sliderTrack.style.transform = `translateX(${currentTranslate - currentPosition}px)`;
                }
            }
            
            // Función para mostrar notificaciones
            function showNotification(message, type = 'info') {
                const notification = document.createElement('div');
                notification.className = `notification ${type}`;
                notification.innerHTML = `
                    <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
                    <span>${message}</span>
                `;
                
                notification.style.cssText = `
                    position: fixed;
                    top: 100px;
                    right: 20px;
                    background: ${type === 'success' ? '#28a745' : '#17a2b8'};
                    color: white;
                    padding: 15px 20px;
                    border-radius: 8px;
                    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
                    display: flex;
                    align-items: center;
                    gap: 10px;
                    z-index: 9999;
                    animation: slideIn 0.3s ease;
                `;
                
                document.body.appendChild(notification);
                
                setTimeout(() => {
                    notification.style.animation = 'slideOut 0.3s ease';
                    setTimeout(() => {
                        if (notification.parentNode) {
                            notification.parentNode.removeChild(notification);
                        }
                    }, 300);
                }, 3000);
                
                // Añadir estilos de animación si no existen
                if (!document.querySelector('#notification-styles')) {
                    const style = document.createElement('style');
                    style.id = 'notification-styles';
                    style.textContent = `
                        @keyframes slideIn {
                            from { transform: translateX(100%); opacity: 0; }
                            to { transform: translateX(0); opacity: 1; }
                        }
                        @keyframes slideOut {
                            from { transform: translateX(0); opacity: 1; }
                            to { transform: translateX(100%); opacity: 0; }
                        }
                    `;
                    document.head.appendChild(style);
                }
            }
        });
    </script>
</body>
</html>